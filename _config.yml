title: "Oční optika a ordinace Řevnice"
description: "Oční optika a oční ambulance v Řevnicích a Dejvicích. Měření zraku, výroba brýlí, kontaktní čočky, OCT vyšetření."
lang: cs
encoding: utf-8

url: ""
baseurl: ""

plugins:
  - jekyll-seo-tag

# Default front matter
defaults:
  - scope:
      path: ""
    values:
      layout: "default"

# Exclude from build
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - LICENSE
